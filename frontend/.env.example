# Frontend Configuration
# Leave empty or set to / in production when reverse-proxied by Nginx
# Or specify full backend URL for local development (e.g., http://localhost:8000)
VITE_API_URL=http://localhost:8000
